import { FilterSpellSearchPipe } from './filter-spell-search-pipe';

describe('FilterSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSpellSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
